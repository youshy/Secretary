package main

// scanDirectory looks for all calls to os.Getenv in every *.go file
// in the directory and outputs the keys.
func scanDirectory(directory string) ([]string, error) {
	return nil, nil
}

// scanFiles looks for all calls to os.Getenv in every *.go file
// provided in the input and outputs the keys.
func scanFiles(files ...string) ([]string, error) {
	return nil, nil
}
